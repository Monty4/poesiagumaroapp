import React from 'react';
import ReactDOM from 'react-dom';
import PoesiagumaroApp from './App';

ReactDOM.render(
    <React.StrictMode>
        <PoesiagumaroApp />
    </React.StrictMode>
    ,
    document.getElementById('root')
    );